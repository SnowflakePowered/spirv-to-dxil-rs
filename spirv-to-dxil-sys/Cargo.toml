[package]
name = "spirv-to-dxil-sys"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]


[build-dependencies]
bindgen = "0.63.0"
cmake = "0.1"

[package.metadata.docs.rs]
targets = []
features = ["docsrs"]